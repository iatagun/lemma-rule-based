# v2: Engine token'ları (dizge fonemleri + kural vurgusu M1a + noktalama, sözcük ayracı token'lı). Ortak ayarlar train_base.yaml'dan.
base: train_base.yaml
name: v2_engine_m1a_e150
frontend: engine
train:
  epochs: 150
