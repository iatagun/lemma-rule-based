# DizgeBERT fine-tuning stack — ELECTRA modelleri (Morph / Joint / Dep / Idiom),
# veri pipeline'ları, HF paketleme, benchmark.

torch>=2.8
transformers>=4.57
conllu
numpy
tqdm
scikit-learn
huggingface_hub>=0.36
safetensors
snowballstemmer          # TDK deyim örneklerinde gövde-eşleştirme (data/prepare_tdk_*.py)
